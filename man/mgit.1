.TH MGIT 1 "2026-07-30" "Knowledge Islands" "User Commands"
.SH NAME
mgit \- run commands across multiple Git repositories
.PP
.SH SYNOPSIS
.SS General
.TP
.B mgit
List the repositories in the current set.
.TP
.B mgit [\fIoptions\fR] [\fIcommand\fR ...]
Run
.B git
with the supplied command and arguments in every repository in the current set.
.TP
.B mgit [\fIoptions\fR] \-B \fIcommand\fR [\fIargument\fR ...]
Run a command directly, without prefixing it with
.BR git ,
in every repository in the current set.
.SS Workspace management
.TP
.B mgit register
Generate
.I .mgit-workspace.toml
manifests for the current tree.
.TP
.B mgit repair
Clone missing repositories declared by structural default groups in the current
.I .mgit-workspace.toml
manifest.
.SS Repository management
.TP
.B mgit structure \fIstandard|nested\fR \fB\-\-dry-run|\-\-yes\fR
Convert every eligible repository in the current set between standard and nested structures.
.TP
.B mgit worktree \fIlist|status\fR
List worktrees or show their branch and cleanliness.
.TP
.B mgit worktree add \fIBRANCH\fR
Add a linked checkout for an existing remote branch in every structured repository.
.TP
.B mgit worktree remove \fIPATH\fR \fB\-\-yes\fR [\fB\-\-force\fR]
Remove one linked worktree.
.SS Shell integration
.TP
.B mgit completion \fIbash|zsh\fR
Print shell completion source.
.SH DESCRIPTION
.B mgit
runs a Git command, or an arbitrary command with
.BR \-B ,
across a set of repositories.
Before each invocation it prints the repository path and command.
With no command it prints the repository paths that would be used.
.PP
The set comes from a
.I .mgit-workspace.toml
in the current directory when one is present.
Otherwise,
.B mgit
walks the current tree for Git repositories, omitting repositories nested inside another repository.
The
.B \-I
option always uses discovery instead of manifests.
.SH OPTIONS
.TP
.BR \-P , " \-\-physical"
Do not follow symlinked container directories.
This is the default.
Symlinked repositories are never followed.
.TP
.BR \-L , " \-\-follow-symlinks"
Follow symlinked container directories while discovering repositories.
.TP
.BR \-B , " \-\-bare"
Run the supplied command directly rather than as a
.B git
subcommand.
.TP
.BR \-I , " \-\-ignore"
Ignore
.I .mgit-workspace.toml
files and discover repositories by walking the tree.
.TP
.BR \-g , " \-\-group" " \fINAME\fR"
Select group
.I NAME
from the workspace in the current directory instead of that workspace's configured default.
Child workspaces use their own configured defaults.
.TP
.BR \-f , " \-\-filter" " \fIGLOB\fR"
Limit the repository set to repositories whose path, relative to the current
directory, matches the shell glob
.IR GLOB .
A bare pattern also matches a repository's last path component, so
.B mgit \-f 'mcp\-*'
selects the mcp repositories wherever they sit in the tree.
The option is repeatable; a repository matching any pattern is kept.
Workspace selection occurs before filtering.
The filter narrows normal commands, the repository listing, and the
.B structure
and
.B worktree
set commands; it does not affect
.B register
or
.BR repair .
.TP
.BR \-h , " \-\-help"
Print usage information and exit.
.TP
.BR \-V , " \-\-version"
Print the version and exit.
.SH COMMANDS
.SS register
.PP
.B mgit register
scans the current tree and writes a
.I .mgit-workspace.toml
in every non-Git container directory, listing child workspaces and repositories.
It writes a
.I .mgit-config.toml
only in a repository that owns cross-repository symlinks.
The command does not descend into repositories, always performs a fresh scan, and replaces generated structural default-group records.
Existing non-default groups are preserved.
.PP
When Chezmoi is installed and configured, generated manifests below its target directory are added to Chezmoi's source state.
.SS repair
.PP
.B mgit repair
uses the workspace manifest in the current directory to clone every missing repository in structural default groups with a source URL.
It creates standard, nested, and bare repositories in their declared forms.
It recursively follows child workspaces through their structural default groups.
Existing paths are never replaced: a present repository must match its declared structure, and child workspace directories and manifests must already exist.
.SS structure
.PP
.B mgit structure nested
converts each eligible clean, attached standard checkout to a nested repository.
.B mgit structure standard
performs the reverse conversion only when the nested repository has its required
.I main/
checkout and no other linked worktrees.
.PP
Both forms require exactly one of
.B \-\-dry-run
to preview changes or
.B \-\-yes
to make them.
.SS worktree
.PP
.B mgit worktree list
groups linked worktrees by their shared Git store.
.B mgit worktree status
shows each worktree's branch and cleanliness.
.PP
.B mgit worktree add \fIBRANCH\fR
adds a checkout for an existing
.B origin/\fIBRANCH\fR
to every structured repository after preflighting the full set.
.B mgit worktree remove \fIPATH\fR \fB\-\-yes\fR
removes one linked checkout and never removes the primary or required
.I main/
checkout.
Pass
.B \-\-force
to discard dirty changes in that linked checkout.
.SS completion
.PP
.B mgit completion bash
and
.B mgit completion zsh
print shell completion definitions to standard output.
For example, source the Bash definition with:
.PP
.nf
source <(mgit completion bash)
.fi
.SH WORKSPACE MANIFESTS
A
.I .mgit-workspace.toml
manifest uses schema 1 and ordered member records in named groups.
The complete generated shape is:
.PP
.nf
schema = 1
default = "default"

[[groups.default.members]]
kind = "repository"
path = "platform"
type = "standard"
source = "git@github.com:acme/platform.git"

[[groups.default.members]]
kind = "repository"
path = "workspace"
type = "nested"

[[groups.default.members]]
kind = "repository"
path = "archive.git"
type = "bare"

[[groups.default.members]]
kind = "workspace"
path = "group"

[[groups.ci.members]]
kind = "repository"
path = "platform"
.fi
.PP
The structural
.B default
group is required.
Its repository members require
.BR type ,
which is one of
.BR standard ,
.BR nested ,
or
.BR bare .
The optional
.B source
field is a clone URL and applies to default-group repository members.
Workspace members use only
.B kind
and
.BR path .
Non-default group repository members also use only
.B kind
and
.BR path ;
they select already-present repositories and do not declare structure or clone sources.
.PP
Member paths are safe relative paths below the manifest directory.
The current workspace uses its configured default group, or a group named with
.BR \-g / \-\-group .
Child workspaces use their own configured defaults.
.PP
Repository-owned
.I .mgit-config.toml
files have a separate leaf-only purpose: their
.B symlinks
table maps a symlink the current repository owns to its target in another repository:
.PP
.nf
[symlinks]
"link" = "target"
.fi
.PP
Blank lines and comments beginning with
.B #
are ignored.
.SH REPOSITORY STRUCTURES
A standard repository is an ordinary checkout with a
.I .git/
directory.
A nested repository is an
.B mgit
controller containing a
.I .bare/
shared Git store, a
.I .git
controller file, and a required
.I main/
checkout.
Commands operate in live checkouts, never in the nested controller or bare store.
.SH FILES
.TP
.I .mgit-workspace.toml
Optional schema-1 workspace manifest in a non-Git container directory.
.TP
.I .mgit-config.toml
Optional repository-leaf symlink metadata.
.SH EXIT STATUS
.TP
.B 0
The requested operation completed successfully.
.TP
.B 1
An operation failed.
.TP
.B 2
The command grammar was invalid, a required confirmation was absent, or an unsupported option was supplied.
.PP
For a fan-out command,
.B mgit
returns the exit status of the last command it invokes.
.SH SEE ALSO
.BR git (1),
.BR git-worktree (1)
.PP
The project README and the worktree structures guide are available at
.I https://github.com/knowledgeislands/tools-mgit .
